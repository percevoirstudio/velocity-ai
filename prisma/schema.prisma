generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// 👤 NOTRE TABLE UTILISATEUR
model User {
  id        String   @id
  credits   Int      @default(3)
  createdAt DateTime @default(now())
}